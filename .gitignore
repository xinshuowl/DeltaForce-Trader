# ─── Python ───────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
*.egg

# ─── 构建产物 (PyInstaller / setup.py) ────────────────
build/
dist/
*.spec.bak

# ─── IDE / 编辑器 ─────────────────────────────────────
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# ─── 用户运行时数据 (每个用户首次运行时生成) ──────────
user_config.json
logs/
listings/
debug_cells/
debug_ocr/

# ─── 道具采集本地截图 (用户自建, 体积大) ──────────────
Shop/

# ─── ML 训练样本 (用户可通过审核标注重新积累) ─────────
# 模型本身在 models/, 已包含, 可开箱即用; samples 仅在重训时需要
samples/

# ─── 调试 / 临时截图 (项目根目录的散落 PNG) ───────────
# 项目根目录历史遗留的几百个调试截图, 不应进入仓库
*.png
!docs/screenshots/*.png

# ─── 文档目录里特定的合法图片要保留, 见上面的 ! 否定规则
# 任何放到 docs/screenshots/ 的图都会被白名单放行

# ─── 私人调研/草稿目录 ────────────────────────────────
Chatgpt/

# ─── 临时日志和文本 ───────────────────────────────────
*.log
sb.txt
sc.txt
提示优化.txt
流程优化.txt
nul

# ─── 早期开发草稿 / 被替代的旧脚本 ────────────────────
debug_find_window.py
xiajia10.py

# ─── 临时验证目录 ─────────────────────────────────────
templates/

# ─── 工具目录的调试文件 (保留 *.py) ───────────────────
tools/debug_*.png
tools/__pycache__/
